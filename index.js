// const users = [];

// // Join user to chat
// function userJoin(id, username, room) {
//   const user = { id, username, room }; // Username and room we get from event emitted inside index.ejs

//   users.push(user); // then we push the user;

//   return user; // and return it 
// }

// module.exports = {
//   userJoin
// };